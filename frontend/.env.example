# Frontend environment variables (copy to `.env`)

# Backend base URL (no trailing slash)
# example (local): http://localhost:8080
VITE_BACKEND_URL=

# Shared API key sent as X-API-Key header (must match backend API_KEY)
VITE_API_KEY=

